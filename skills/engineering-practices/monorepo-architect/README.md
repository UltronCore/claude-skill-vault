# Monorepo Architect

Design efficient monorepos with Nx, Turborepo, or Bazel. Workspace configuration, remote caching, affected builds, shared libraries, and CI optimization.

## Activation
`/monorepo-architect` — activate when setting up or optimizing a monorepo, configuring build caching, or structuring multi-package workspaces.
