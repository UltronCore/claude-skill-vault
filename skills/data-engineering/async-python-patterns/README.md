# Async Python Patterns

Async Python with asyncio: event loops, coroutines, task groups, concurrent HTTP, async generators, and avoiding common pitfalls.

## Activation
`/async-python-patterns` — activate when writing async Python services, concurrent scrapers, or high-throughput IO-bound code.
