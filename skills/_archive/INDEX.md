# _archive — Index

**Last cleaned:** 2026-05-24

## Status

This directory is empty. All 73 skills that were previously archived here were confirmed to have active counterparts in the live skill tree and were deleted on 2026-05-24.

## What was here (and why it was removed)

All archived entries fell into one of these categories:

| Category | Count | Example | Action |
|---|---|---|---|
| Exact duplicates (same folder name) | 44 | `brainstorming`, `firecrawl`, `test-driven-development` | Deleted — active copy exists |
| Renamed duplicates (`-backend-dup` suffix) | 7 | `clean-architecture-backend-dup`, `functional-python-backend-dup` | Deleted — active copy in `engineering-practices/` |
| Renamed duplicates (`-devops-dup` suffix) | 5 | `container-security-devops-dup`, `distributed-tracing-cloud-devops-dup` | Deleted — active copy in category |
| Renamed duplicates (`-data-dup` suffix) | 3 | `database-migration-strategies-data-dup`, `feature-store-engineering-ai-ml-dup` | Deleted — active copy in `data-engineering/` |
| Stub files (`-stub` suffix) | 5 | `dns-infrastructure-devops-stub`, `swiftui-data-flow-ios-mobile-stub` | Deleted — full active copy exists |
| Other duplicates | 9 | `multi-agent-orchestration-orchestration-dup`, `postgres-sharding-data-dup` | Deleted — active copy in category |

## How to use _archive going forward

If you need to retire a skill:
1. Verify no active counterpart exists first.
2. Move the skill folder here with a suffix that explains why (e.g., `-superseded-by-X`).
3. Add a row to the table above with the date, skill name, and reason.
4. Commit and push.

Do not archive skills that have active counterparts — just delete them.
